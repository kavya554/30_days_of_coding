#include <bits/stdc++.h>

using namespace std;



int main()
{
    string S;
    int n;
    getline(cin, S);
    try {
        n=stoi(S);
        cout<<S;
    } catch (...) {
    cout<<"Bad String";
    }

    return 0;
}
